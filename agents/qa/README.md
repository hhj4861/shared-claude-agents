# QA Pipeline

Claude Code 기반 자동화된 QA 테스트 파이프라인입니다.

---

## 목차

1. [전체 Flow](#1-전체-flow)
2. [사전 조건](#2-사전-조건)
3. [각 스텝별 진행 과정](#3-각-스텝별-진행-과정)
4. [결과 확인 방법](#4-결과-확인-방법)
5. [명령어 가이드](#5-명령어-가이드)
6. [참조](#6-참조)

---

# 1. 전체 Flow

## 1.1 QA Pipeline 전체 구조

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              QA Pipeline Flow                                │
└─────────────────────────────────────────────────────────────────────────────┘

 ┌──────────────┐
 │  사용자 요청  │
 │ "/qa-scenario"│
 │ "/e2e-test"   │
 │ "/api-test"   │
 └──────┬───────┘
        │
        ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │                        Phase 1: 시나리오 생성                              │
 │                           /qa-scenario                                    │
 │                                                                          │
 │   Step 1          Step 1.5         Step 2          Step 3                │
 │  ┌────────┐      ┌────────┐      ┌────────┐      ┌────────┐             │
 │  │ 문서   │      │프로젝트│      │ 코드   │      │시나리오│             │
 │  │ 수집   │ ──▶  │ 구조   │ ──▶  │ 분석   │ ──▶  │ 작성   │             │
 │  │        │      │ 분석   │      │        │      │        │             │
 │  └────────┘      └────────┘      └────────┘      └────────┘             │
 │       │               │               │               │                  │
 │       ▼               ▼               ▼               ▼                  │
 │  references/    project-       be-analysis.md   e2e-scenarios.md        │
 │  *.md           structure.json fe-analysis.md   api-scenarios.md        │
 │                                                                          │
 │                    [선택적] Step 3.5 → Step 3.6                          │
 │                    Gemini 검토 → 피드백 반영                              │
 └──────────────────────────────────────────────────────────────────────────┘
        │
        ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │                        Phase 2: 테스트 실행                               │
 │                                                                          │
 │  ┌────────────────────────┐      ┌────────────────────────┐             │
 │  │      /e2e-test         │      │      /api-test         │             │
 │  │                        │      │                        │             │
 │  │  Playwright MCP로      │      │  HTTP 요청으로         │             │
 │  │  브라우저 자동화       │      │  API 검증              │             │
 │  │                        │      │                        │             │
 │  │  - 로그인              │      │  - 엔드포인트 호출     │             │
 │  │  - 화면 조작           │      │  - 응답 검증           │             │
 │  │  - 결과 검증           │      │  - 에러 케이스         │             │
 │  └────────────────────────┘      └────────────────────────┘             │
 │              │                              │                            │
 │              ▼                              ▼                            │
 │     대시보드 (localhost:3847)       테스트 리포트                         │
 │     실시간 진행 상황                 reports/*.md                        │
 └──────────────────────────────────────────────────────────────────────────┘
        │
        ▼
 ┌──────────────┐
 │   결과 확인   │
 │              │
 │ - 대시보드   │
 │ - PDF 리포트 │
 │ - Jira 티켓  │
 └──────────────┘
```

## 1.2 주요 명령어와 역할

| 명령어 | 역할 | 결과물 |
|--------|------|--------|
| `/qa-scenario` | 테스트 시나리오 자동 생성 | `docs/qa/latest/scenarios/*.md` |
| `/e2e-test` | 브라우저 E2E 테스트 실행 | 대시보드 + 리포트 |
| `/api-test` | API 테스트 실행 | 테스트 리포트 |

---

# 2. 사전 조건

## 2.1 공통 사전 조건

### 필수 설치 항목

| 항목 | 설명 | 확인 방법 |
|------|------|----------|
| Claude Code | CLI 도구 | `claude --version` |
| Node.js | v18 이상 | `node --version` |
| Git | 버전 관리 | `git --version` |

### Atlassian MCP (Confluence/Jira 연동 시)

```bash
# Atlassian MCP 등록
claude mcp add -s user --transport sse atlassian https://mcp.atlassian.com/v1/sse

# Claude Code 재시작 후 /mcp → atlassian 선택하여 OAuth 인증
```

## 2.2 /qa-scenario 실행 전 준비사항

### 필요한 정보

| 항목 | 설명 | 예시 |
|------|------|------|
| BE 프로젝트 경로 | 백엔드 소스 경로 | `/path/to/backend` |
| FE 프로젝트 경로 | 프론트엔드 소스 경로 | `/path/to/frontend` |
| PRD 문서 URL | Confluence 또는 문서 URL | `https://confluence.example.com/...` |
| API 명세서 URL | Swagger 또는 API 문서 URL | `https://api.example.com/swagger` |
| 테스트 서버 URL | 테스트 대상 URL | `https://test.example.com` |

### 방법 1: 웹 입력 폼으로 설정 (권장)

웹 브라우저에서 직관적인 UI로 설정을 입력할 수 있습니다.

```bash
node ~/.claude/scripts/qa-input-form/index.js
```

브라우저가 자동으로 열리며, 폼에서 다음 정보를 입력합니다:

```
┌─────────────────────────────────────────────────────────────────┐
│  QA 시나리오 생성 설정                                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📁 프로젝트 경로                                                │
│  ├── BE 프로젝트 경로: /path/to/backend                         │
│  └── FE 프로젝트 경로: /path/to/frontend                        │
│                                                                 │
│  🌐 테스트 대상 서버                                             │
│  ├── 프론트엔드 URL: https://dev.example.com                    │
│  └── 백엔드 API URL: https://api-dev.example.com                │
│                                                                 │
│  🔐 인증 방식                                                    │
│  ├── Keycloak SSO (ID/PW + OTP)                                 │
│  ├── JWT 토큰                                                   │
│  └── 인증 없음                                                   │
│                                                                 │
│  📋 참조 문서 (여러 개 가능)                                     │
│  ├── 기능정의서 (PRD): Confluence URL                           │
│  ├── API 명세서: Swagger URL                                    │
│  ├── 화면설계서: Figma URL                                      │
│  └── 정책서: 보안/권한 문서 URL                                  │
│                                                                 │
│  [🚀 시나리오 생성 시작]                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**결과:**
- `docs/qa/latest/config.json` 자동 생성
- `docs/qa/latest/references/`, `analysis/`, `scenarios/` 디렉토리 구조 자동 생성
- 기존 `latest/`가 있으면 `history/`로 자동 백업

### 방법 2: config.json 직접 작성

JSON 파일을 직접 작성하여 `{프로젝트}/docs/qa/latest/config.json`에 저장:

```json
{
  "project_name": "프로젝트명",
  "be_path": "/path/to/backend",
  "fe_path": "/path/to/frontend",
  "documents": [
    {
      "name": "PRD",
      "type": "confluence",
      "url": "https://confluence.example.com/pages/..."
    },
    {
      "name": "API 명세서",
      "type": "swagger",
      "url": "https://api.example.com/swagger"
    }
  ],
  "test_server": {
    "fe_url": "https://test.example.com",
    "api_url": "https://api.test.example.com"
  },
  "auth": {
    "type": "keycloak",
    "username": "test@example.com",
    "password": "password",
    "otp_method": "manual"
  }
}
```

## 2.3 /e2e-test 실행 전 준비사항

| 체크 항목 | 확인 방법 |
|-----------|----------|
| 시나리오 생성 완료 | `docs/qa/latest/scenarios/e2e-scenarios.md` 존재 |
| config.json 존재 | `docs/qa/latest/config.json` 존재 |
| 테스트 서버 실행 중 | 브라우저에서 테스트 URL 접속 가능 |
| 로그인 계정 확인 | config.json의 auth 설정 확인 |

## 2.4 /api-test 실행 전 준비사항

| 체크 항목 | 확인 방법 |
|-----------|----------|
| API 시나리오 생성 완료 | `docs/qa/latest/scenarios/api-scenarios.md` 존재 |
| API 서버 실행 중 | API 엔드포인트 접근 가능 |
| 인증 토큰 (필요 시) | config.json의 auth 설정 확인 |

---

# 3. 각 스텝별 진행 과정

## 3.1 Phase 1: 시나리오 생성 (/qa-scenario)

### Step 1: 문서 수집

```
┌─────────────────────────────────────────────────────────────────┐
│  Step 1: 문서 수집                                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  입력: config.json의 documents URL 목록                         │
│                                                                 │
│  처리:                                                          │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐       │
│  │ Confluence  │     │   Swagger   │     │  PDF/DOCX   │       │
│  │ 문서 수집   │     │  API 수집   │     │   변환      │       │
│  └──────┬──────┘     └──────┬──────┘     └──────┬──────┘       │
│         │                   │                   │               │
│         └───────────────────┴───────────────────┘               │
│                             │                                   │
│                             ▼                                   │
│                     Markdown 변환                               │
│                             │                                   │
│                             ▼                                   │
│  출력: docs/qa/latest/references/                               │
│        ├── prd/*.md         (PRD 문서)                          │
│        ├── api/*.md         (API 명세서)                        │
│        └── policy/*.md      (정책 문서)                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Step 1.5: 프로젝트 구조 분석

```
┌─────────────────────────────────────────────────────────────────┐
│  Step 1.5: 프로젝트 구조 분석                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  입력: BE/FE 프로젝트 경로                                       │
│                                                                 │
│  처리:                                                          │
│  ┌─────────────────────┐     ┌─────────────────────┐           │
│  │   BE 프로젝트 감지   │     │   FE 프로젝트 감지   │           │
│  │                     │     │                     │           │
│  │ - build.gradle      │     │ - package.json      │           │
│  │ - pom.xml           │     │ - vue/react/angular │           │
│  │ - requirements.txt  │     │ - vite/next/nuxt    │           │
│  └──────────┬──────────┘     └──────────┬──────────┘           │
│             │                           │                       │
│             └───────────┬───────────────┘                       │
│                         ▼                                       │
│              프레임워크별 분석 패턴 생성                          │
│                         │                                       │
│                         ▼                                       │
│  출력: docs/qa/latest/analysis/project-structure.json           │
│        {                                                        │
│          "be": { "framework": "spring-boot", ... },             │
│          "fe": { "framework": "vue3", ... }                     │
│        }                                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Step 2: 코드 분석

```
┌─────────────────────────────────────────────────────────────────┐
│  Step 2: 코드 분석                                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  입력: project-structure.json + 소스 코드                       │
│                                                                 │
│  BE 분석:                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ - Controller 파일 스캔                                    │   │
│  │ - API 엔드포인트 추출 (@GetMapping, @PostMapping 등)      │   │
│  │ - DTO, 응답 스키마 분석                                   │   │
│  │ - 비즈니스 로직 파악                                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  FE 분석:                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ - 라우터 파일 스캔                                        │   │
│  │ - 페이지 컴포넌트 분석                                    │   │
│  │ - API 호출 패턴 추출 (axios, fetch)                       │   │
│  │ - UI 컴포넌트 식별 (테이블, 폼, 모달)                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  출력: docs/qa/latest/analysis/                                 │
│        ├── be-analysis.md      (BE API 분석 결과)               │
│        ├── fe-analysis.md      (FE 화면 분석 결과)              │
│        └── test-targets.json   (테스트 대상 목록)               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Step 3: 시나리오 작성

```
┌─────────────────────────────────────────────────────────────────┐
│  Step 3: 시나리오 작성                                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  입력: references/ + analysis/ 전체                             │
│                                                                 │
│  처리:                                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. PRD 요구사항 매핑                                      │   │
│  │ 2. API/UI 기능별 TC 생성                                  │   │
│  │ 3. 우선순위 분류 (P0~P2)                                  │   │
│  │ 4. 상세 스텝 정의                                         │   │
│  │ 5. 예상 결과 및 검증 조건 작성                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  TC ID 규칙: TC-{GROUP}-E2E-{번호}                              │
│  예: TC-AUTH-E2E-001, TC-MENU-E2E-001                           │
│                                                                 │
│  출력: docs/qa/latest/scenarios/                                │
│        ├── e2e-scenarios.md    (E2E 테스트 시나리오)            │
│        ├── api-scenarios.md    (API 테스트 시나리오)            │
│        └── edge-cases.md       (엣지 케이스 시나리오)           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Step 3.5~3.6: 시나리오 검토 및 보완 (선택적)

```
┌─────────────────────────────────────────────────────────────────┐
│  Step 3.5: 외부 검토 (Gemini CLI) - 선택적                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Gemini CLI가 설치된 경우에만 실행                               │
│                                                                 │
│  검토 항목:                                                     │
│  ├── 요구사항 커버리지 (누락 확인)                              │
│  ├── 코드 커버리지 (API/UI 누락 확인)                           │
│  ├── 중복 테스트 검출                                           │
│  ├── 불필요한 테스트 식별                                       │
│  └── 추가 테스트 기회 제안                                      │
│                                                                 │
│  출력: docs/qa/latest/review/scenario-review.md                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 3.6: 피드백 반영 - 선택적                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 3.5 검토 결과가 있는 경우에만 실행                         │
│                                                                 │
│  보완 작업:                                                     │
│  ├── 누락 시나리오 추가                                         │
│  ├── 중복 테스트 제거                                           │
│  ├── 불필요한 테스트 다운그레이드                               │
│  └── 추가 테스트 케이스 생성                                    │
│                                                                 │
│  출력: scenarios/*.md (최종 보완본)                             │
│        scenarios/refinement-log.md (보완 내역)                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 3.2 Phase 2: E2E 테스트 실행 (/e2e-test)

### 실행 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│  E2E 테스트 실행 흐름                                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 0: 환경 준비                                              │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. config.json 읽기 (테스트 URL, 인증 정보)              │   │
│  │ 2. 대시보드 자동 시작 (localhost:3847)                   │   │
│  │ 3. 시나리오 로드                                         │   │
│  │ 4. 인증 상태 확인 (e2e_check_auth)                       │   │
│  │    - valid: true → 로그인 스킵                           │   │
│  │    - valid: false → 로그인 필요                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  Step 1: 로그인 (필요 시)                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. 테스트 URL 접속                                       │   │
│  │ 2. Keycloak 로그인 페이지 → ID/PW 입력                   │   │
│  │ 3. OTP 입력 (manual 모드 시 사용자 입력 대기)            │   │
│  │ 4. 인증 상태 저장 (다음 실행 시 재사용)                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  Step 2: TC 순차 실행                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 각 TC마다:                                               │   │
│  │ 1. TC 시작 → 대시보드 동기화                             │   │
│  │ 2. 시나리오 스텝 순차 실행:                              │   │
│  │    - browser_snapshot() → 페이지 상태 확인               │   │
│  │    - browser_click/type/navigate → 액션 실행             │   │
│  │    - 대시보드 동기화 ($SYNC step)                        │   │
│  │ 3. TC 완료 → 대시보드 동기화                             │   │
│  │ 4. 다음 TC로 이동                                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  Step 3: 결과 리포트 생성                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. e2e_create_report() 호출                              │   │
│  │ 2. 테스트 결과 요약                                      │   │
│  │ 3. 실패 TC 목록 및 원인                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  Step 4: Jira 티켓 생성 (실패 TC가 있는 경우)                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. 실패 TC 확인 (/api/summary)                           │   │
│  │ 2. Jira 프로젝트 키 질문 (또는 상위 이슈 키)             │   │
│  │ 3. 각 실패 TC에 대해 티켓 생성                           │   │
│  │    - Summary: [E2E] TC-XXX: 테스트명 - 실패              │   │
│  │    - Description: 실패 원인, 스텝, 개선 방안             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 3.3 Phase 2: API 테스트 실행 (/api-test)

```
┌─────────────────────────────────────────────────────────────────┐
│  API 테스트 실행 흐름                                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. api-scenarios.md 로드                                       │
│  2. 각 TC의 API 요청 실행:                                      │
│     - HTTP 메서드 (GET/POST/PUT/DELETE)                         │
│     - 요청 헤더, 바디                                           │
│     - 응답 상태 코드 검증                                       │
│     - 응답 바디 검증                                            │
│  3. 결과 리포트 생성                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

# 4. 결과 확인 방법

## 4.1 시나리오 생성 결과 (/qa-scenario)

### 결과 파일 위치

```
docs/qa/latest/
├── config.json              # 설정 파일
├── config-state.json        # 파이프라인 상태
│
├── references/              # Step 1 결과: 수집된 문서
│   ├── prd/*.md
│   ├── api/*.md
│   └── index.md
│
├── analysis/                # Step 1.5~2 결과: 분석 결과
│   ├── project-structure.json
│   ├── be-analysis.md
│   ├── fe-analysis.md
│   └── test-targets.json
│
├── scenarios/               # Step 3 결과: 테스트 시나리오
│   ├── e2e-scenarios.md     ← E2E 테스트용
│   ├── api-scenarios.md     ← API 테스트용
│   └── edge-cases.md
│
└── review/                  # Step 3.5 결과: 검토 결과 (선택적)
    └── scenario-review.md
```

### 시나리오 파일 구조 예시

```markdown
## 1. 인증 시나리오

### TC-AUTH-E2E-001: Keycloak 로그인 성공

**우선순위**: P0 (Critical)

**사전 조건**:
- 유효한 테스트 계정 존재
- 테스트 서버 실행 중

**테스트 스텝**:

| # | 액션 | 요소 식별 | 입력값/기대값 | 예상 상태 변화 |
|---|------|----------|--------------|---------------|
| 1 | navigate | /login | - | 로그인 페이지 표시 |
| 2 | fill | 이메일 입력 필드 | test@example.com | 입력값 표시 |
| 3 | fill | 비밀번호 입력 필드 | ******** | 마스킹 표시 |
| 4 | click | "로그인" 버튼 | - | OTP 페이지 이동 |
| 5 | fill | OTP 입력 필드 | 123456 | 입력값 표시 |
| 6 | click | "확인" 버튼 | - | 대시보드 이동 |

**예상 결과**:
- 대시보드 페이지가 표시됨
- 사용자 이름이 헤더에 표시됨
```

## 4.2 E2E 테스트 결과 (/e2e-test)

```bash
node ~/.claude/scripts/e2e-dashboard/index.js
```

### 실시간 대시보드

```
┌─────────────────────────────────────────────────────────────────┐
│  E2E Test Dashboard                                              │
│  http://localhost:3847                                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  전체: 46    성공: 38    실패: 3    진행중: 1    대기: 4   │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─────────────────┐  ┌─────────────────────────────────────┐ │
│  │ 그룹별 TC 목록   │  │         진행 상황                    │ │
│  │                 │  │                                     │ │
│  │ ▼ AUTH (3)      │  │  [=========>          ] 85%         │ │
│  │   ✅ TC-001     │  │                                     │ │
│  │   ✅ TC-002     │  │  현재: TC-MENU-E2E-005               │ │
│  │   ✅ TC-003     │  │  스텝: 3/5                          │ │
│  │                 │  │                                     │ │
│  │ ▼ MENU (10)     │  │  ┌───────────────────────────────┐ │ │
│  │   ✅ TC-001     │  │  │  그룹별 통과율                 │ │ │
│  │   ✅ TC-002     │  │  │                               │ │ │
│  │   ❌ TC-003     │  │  │  AUTH   ████████████  100%    │ │ │
│  │   🔄 TC-004     │  │  │  MENU   ████████░░░░   80%    │ │ │
│  │   ⬜ TC-005     │  │  │  CLIENT ████████████  100%    │ │ │
│  │   ...           │  │  │                               │ │ │
│  │                 │  │  └───────────────────────────────┘ │ │
│  └─────────────────┘  └─────────────────────────────────────┘ │
│                                                                 │
│  [PDF 다운로드]                                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### PDF 리포트 다운로드

대시보드에서 "PDF 다운로드" 버튼 클릭:

```
e2e-test-report-2026-01-14.pdf

내용:
- 테스트 요약 (전체/성공/실패/통과율)
- 그룹별 결과 테이블
- 실패 TC 상세 (원인, 실패 스텝)
```

### 실패 TC → Jira 티켓

테스트 완료 후 실패 TC가 있으면:

```
1. 실패 TC 5건 발견
2. "Jira 티켓을 생성할까요?" 질문
3. 프로젝트 키 입력 (예: SYSTAN-155)
4. 하위 작업으로 티켓 생성

결과:
├── SYSTAN-156: [E2E] TC-AUTH-E2E-001: 로그인 실패
├── SYSTAN-157: [E2E] TC-MENU-E2E-003: 메뉴 등록 실패
└── ...
```

## 4.3 API 테스트 결과 (/api-test)

```
docs/qa/latest/reports/
└── api-test-report-*.md

내용:
- API 테스트 요약
- 엔드포인트별 결과
- 실패 케이스 상세
```

## 4.4 결과 확인 요약

| 결과 유형 | 확인 위치 | 확인 방법 |
|----------|----------|----------|
| 시나리오 | `docs/qa/latest/scenarios/` | 파일 열기 |
| E2E 실시간 진행 | http://localhost:3847 | 브라우저 접속 |
| E2E 리포트 | 대시보드 PDF 다운로드 | 버튼 클릭 |
| 실패 티켓 | Jira | 생성된 티켓 확인 |
| API 리포트 | `docs/qa/latest/reports/` | 파일 열기 |

---

# 5. 명령어 가이드

## 5.1 슬래시 명령어

| 명령어 | 설명 | 예시 |
|--------|------|------|
| `/qa-scenario` | 전체 파이프라인 실행 | 시나리오 생성 |
| `/qa-scenario --auto` | 질문 없이 자동 실행 | 자동 생성 |
| `/qa-scenario --from step2` | step2부터 시작 | 코드 분석부터 |
| `/qa-scenario --from step3` | step3만 실행 | 시나리오만 재작성 |
| `/e2e-test` | 전체 E2E 테스트 | 모든 TC 실행 |
| `/e2e-test --auto` | 질문 없이 자동 실행 | 자동 테스트 |
| `/e2e-test --list` | TC 그룹 목록 조회 | 목록만 확인 |
| `/e2e-test TC-001` | 특정 TC만 실행 | TC-001만 |
| `/e2e-test @MENU` | 특정 그룹만 실행 | MENU 그룹만 |
| `/e2e-test @AUTH,CLIENT` | 복수 그룹 실행 | AUTH, CLIENT |
| `/e2e-test --retry` | 재테스트 대상 전체 | 미진행+보류+실패 |
| `/e2e-test --pending` | 미진행 TC만 실행 | waiting TC |
| `/e2e-test --incomplete` | 보류 TC만 재실행 | 완성도 낮음 |
| `/e2e-test --failed` | 실패 TC만 재실행 | failed TC |
| `/e2e-test --failed @MENU` | 그룹 내 실패 TC | MENU 실패만 |
| `/api-test` | API 테스트 실행 | 전체 API 테스트 |
| `/api-test --auto` | 질문 없이 자동 실행 | 자동 테스트 |
| `/api-test TC-API-001` | 특정 TC만 실행 | TC-API-001만 |

## 5.2 재테스트 명령어

테스트 실행 후 미완료/실패 TC를 선택적으로 재실행할 수 있습니다.

### 재테스트 분류

| 옵션 | 대상 | 설명 |
|------|------|------|
| `--retry` | 전체 | 미진행 + 보류 + 실패 TC 모두 |
| `--pending` | 미진행 | 아직 테스트 시도 안 한 TC |
| `--incomplete` | 보류 | 완성도 50% 미만으로 pending 처리된 TC |
| `--failed` | 실패 | 테스트 실패한 TC |

### 사용 예시

```bash
# 모든 재테스트 대상 실행
/e2e-test --retry

# 미진행 TC만 실행
/e2e-test --pending

# 보류(완성도 낮음) TC만 재실행
/e2e-test --incomplete

# 실패 TC만 재실행
/e2e-test --failed

# MENU 그룹 내 실패 TC만 재실행
/e2e-test --failed @MENU

# AUTH, CLIENT 그룹 내 미진행 TC만 실행
/e2e-test --pending @AUTH,CLIENT
```

### 대시보드 API

재테스트 대상은 `/api/summary` 응답에서 확인:

```bash
curl -s http://localhost:3847/api/summary | python3 -c "
import sys, json
d = json.load(sys.stdin)
print(f'미진행: {len(d.get(\"waitingTCs\", []))}개')
print(f'보류:   {len(d.get(\"pendingTCs\", []))}개')
print(f'실패:   {len(d.get(\"failedTCs\", []))}개')
"
```

## 5.3 자연어 요청

| 자연어 | 변환 명령어 |
|--------|-------------|
| "QA 시나리오 만들어줘" | `/qa-scenario` |
| "E2E 테스트 해줘" | `/e2e-test` |
| "메뉴 관리 테스트만" | `/e2e-test @MENU` |
| "실패한 테스트 다시 해줘" | `/e2e-test --failed` |
| "남은 테스트 이어서 해줘" | `/e2e-test --pending` |
| "API 테스트 실행해" | `/api-test` |
| "코드 분석부터 다시" | `/qa-scenario --from step2` |

---

# 6. 참조

## 6.1 에이전트 및 스킬 구성

### 시나리오 생성 (서브에이전트)

| 에이전트 | 역할 | 모델 |
|---------|------|------|
| `qa-director` | QA 파이프라인 총괄 | sonnet |
| `step1-doc-collector` | 문서 수집 | sonnet |
| `step1.5-project-detector` | 프로젝트 구조 분석 | haiku |
| `step2-code-analyzer` | 코드 분석 | sonnet |
| `step3-scenario-writer` | 시나리오 작성 | opus |
| `step3.5-scenario-reviewer` | 시나리오 검토 (Gemini) | haiku |
| `step3.6-scenario-refiner` | 시나리오 보완 | opus |

### 테스트 실행 (스킬 기반)

| 스킬 | 역할 | 실행 방식 |
|------|------|----------|
| `/e2e-test` | E2E 테스트 실행 (Playwright MCP) | 메인 에이전트에서 직접 |
| `/api-test` | API 테스트 실행 | 메인 에이전트에서 직접 |

> **참고**: 테스트 실행은 별도 서브에이전트가 아닌 메인 에이전트 + 스킬로 진행됩니다.
> 이전 `step4-e2e-tester`, `step4-backend-tester` 에이전트는 archived 되었습니다.

## 6.2 디렉토리 구조

```
docs/qa/latest/
├── config.json              # 설정
├── config-state.json        # 상태
├── references/              # 수집 문서
├── analysis/                # 분석 결과
├── scenarios/               # 테스트 시나리오
├── review/                  # 검토 결과
├── playwright/              # 인증 상태
│   └── .auth/user.json
└── reports/                 # 테스트 리포트
```

## 6.3 E2E 대시보드

### 대시보드 실행

```bash
# 시나리오 파일을 포함하여 대시보드 실행
$HOME/.claude/shared-agents/scripts/e2e-dashboard/start.sh "/path/to/docs/qa/latest/scenarios/e2e-scenarios.md"

# 예시: SOCAR 프로젝트
$HOME/.claude/shared-agents/scripts/e2e-dashboard/start.sh "$PROJECT_PATH/docs/qa/latest/scenarios/e2e-scenarios.md"
```

### 테스트 이력 저장소

대시보드는 파일 기반으로 테스트 이력을 자동 저장합니다.

```
저장 위치: $HOME/.claude/shared-agents/scripts/e2e-dashboard/history/

파일 형식: {timestamp}_{project-name}.json
예시: 2026-01-14T15-30-00_socar-backoffice.json
```

### 이력 저장 동작

| 시점 | 동작 |
|------|------|
| TC 완료 시 | 자동 저장 (각 TC 완료마다) |
| 서버 시작 시 | 이전 이력 자동 복원 |
| 수동 저장 | `/api/history/save` 호출 |

### 이력 확인 방법

```bash
# 1. 디렉토리에서 직접 확인
ls -la $HOME/.claude/shared-agents/scripts/e2e-dashboard/history/

# 2. API로 확인 (대시보드 실행 중일 때)
curl http://localhost:3847/api/history

# 3. 최근 이력 내용 확인
cat $HOME/.claude/shared-agents/scripts/e2e-dashboard/history/*.json | jq .
```

### 이력 관련 API

| API | 설명 |
|-----|------|
| `GET /api/history` | 저장된 이력 파일 목록 조회 |
| `POST /api/history/restore` | 특정 이력 파일 복원 |
| `POST /api/history/save` | 현재 상태 수동 저장 |
| `GET /api/summary` | 현재 테스트 진행 상황 요약 |

### 이력 관리

- 최대 20개 파일 유지 (오래된 파일 자동 삭제)
- 프로젝트별 이력 분리 (파일명에 프로젝트명 포함)
- 서버 재시작 시 동일 시나리오 경로의 최신 이력 자동 로드

---

## 6.4 주의사항

### 테스트 데이터 관리

- 생성 데이터: `[E2E]` prefix 사용
- 삭제: 테스트 생성 데이터만
- 운영 데이터: 절대 수정/삭제 금지

### 인증 처리

- 인증 상태는 `playwright/.auth/user.json`에 저장
- 유효한 경우 로그인 스킵
- OTP 수동 입력 시 사용자 대기

---

**마지막 업데이트**: 2026-01-15
